node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.staging
.env.beta
!.env.example
# Ignore stray SQL (e.g. local data dumps) but DO commit committed migrations.
*.sql
!scripts/*.sql
!supabase/migrations/*.sql

# Development scratch files & one-off patches
scratch/
patch*.py

# Local Claude Code / preview tooling config
.claude/

# Simulation output & benchmark data (exposes interview strategy)
output/
benchmarks/
